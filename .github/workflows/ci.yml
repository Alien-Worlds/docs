name: CI

on:
  pull_request:
  push:
    branches: [main]

# A newer push to the same ref makes the in-flight run irrelevant.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Check formatting
        run: pnpm format:check

      - name: Typecheck
        run: pnpm typecheck

      - name: Unit tests
        run: pnpm test

      # Compares every <BlockExplorer*Links> claim in the docs against the
      # cached ABIs. Offline by design: CI must not fail because a WAX node
      # is down. Refreshing the cache is the abi-drift workflow's job.
      - name: Check contract docs against cached ABIs
        run: pnpm abi:check

      # onBrokenLinks: 'throw' means this also validates every internal link.
      - name: Build
        run: pnpm build

      - uses: actions/upload-artifact@v7
        with:
          name: site
          path: build
          retention-days: 7
